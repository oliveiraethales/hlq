hlq - Human Language Query
===

hlq is a simple app that allows you to type SQL queries in a humanized way.

A simple example:
`SELECT * FROM PRODUCTS`
can be type as:
`I want all products`

Simple as that!
